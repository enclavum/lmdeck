# Copy to `.env` (gitignored) and fill in. Never commit real keys.

# Endpoint API key, if you set one in Settings → Security (leave blank for none):
LMDECK_API_KEY=

# Optional overrides:
# LMDECK_BASE_URL=http://localhost:5678/v1
# LMDECK_TEST_MODEL=qwen2.5:7b
# LMDECK_TIMEOUT=120
